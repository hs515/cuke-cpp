#pragma once

#include <string>
#include <utility>
#include <vector>

namespace cuke
{
    namespace internal
    {

        struct ListenerOptions 
        {
            bool consoleReport = false;
            bool htmlReport = false;
        };

    } // namespace internal

} // namespace cuke
